# Copy to .env for local development. On Railway these are env vars.
DATABASE_URL=postgres://user:pass@localhost:5432/all22
APP_PASSWORD=changeme
COOKIE_SECRET=any-long-random-string
PORT=3000
# Claude vision OCR + (later) natural-language Q&A. Without it, screenshot
# reading falls back to the bundled offline OCR (worse accuracy).
ANTHROPIC_API_KEY=
